# Python
__pycache__/
*.pyc
.venv/
venv/

# Секрет сесій — на хостингу задається через змінну середовища SECRET_KEY,
# локальний файл не повинен потрапляти в репозиторій.
.secret_key

# Дані застосунку — генеруються самим сервером при першому запуску,
# містять паролі (хеші) та особисту інформацію користувачів.
users.json
excerpts.json
overview_notes.json
analytics.json

# Кеш завантажених глав Біблії — теж генерується автоматично.
book_cache/

# Системні файли
.DS_Store
Thumbs.db
